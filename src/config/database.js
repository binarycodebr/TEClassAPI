export default {
  //Informe a sua url de conexão com Mongo
  url: "mongodb://<usuario>:<senha>@<url><porta>/<banco>?ssl=true&replicaSet=<replicaSet>&authSource=admin&retryWrites=true&w=majority",
  config: {
    useNewUrlParser: true
  }
}